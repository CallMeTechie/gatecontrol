{% extends theme + "/layout.njk" %}

{% block content %}
<style nonce="{{ cspNonce }}">
.settings-tabs {
  display: flex;
  gap: 0;
  border-bottom: 2px solid var(--border);
  margin-bottom: 20px;
  overflow: visible;
  position: sticky;
  top: 0;
  background: var(--bg-panel);
  z-index: 10;
  flex-shrink: 0;
}
.settings-tabs .tab {
  padding: 10px 18px;
  font-size: 13px;
  font-weight: 500;
  color: var(--text-2);
  cursor: pointer;
  border-bottom: 2px solid transparent;
  margin-bottom: -2px;
  white-space: nowrap;
  border-radius: 0;
  transition: color 0.15s, border-color 0.15s;
}
.settings-tabs .tab:hover {
  color: var(--text-1);
}
.settings-tabs .tab.active {
  color: var(--accent);
  border-bottom-color: var(--accent);
  background: none;
  box-shadow: none;
}
.settings-panel {
  display: flex;
  flex-direction: column;
  gap: 14px;
  max-width: 700px;
}

/* ─── Mobile: Hamburger Dropdown ─────────────────────── */
.settings-tab-toggle {
  display: none;
  align-items: center;
  gap: 10px;
  padding: 10px 14px;
  width: 100%;
  background: var(--bg-panel);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  cursor: pointer;
  color: var(--text-1);
  font-size: 13px;
  font-weight: 500;
}
.settings-tab-toggle svg {
  flex-shrink: 0;
  transition: transform 0.2s;
}
.settings-tab-toggle.open svg {
  transform: rotate(180deg);
}
.settings-tab-dropdown {
  display: none;
  position: absolute;
  top: 100%;
  left: 0;
  right: 0;
  background: var(--bg-panel);
  border: 1px solid var(--border);
  border-top: none;
  border-radius: 0 0 var(--radius-sm) var(--radius-sm);
  box-shadow: var(--shadow);
  z-index: 20;
  flex-direction: column;
}
.settings-tab-dropdown.open {
  display: flex;
}
.settings-tab-dropdown .tab {
  padding: 12px 16px;
  font-size: 13px;
  font-weight: 500;
  color: var(--text-2);
  cursor: pointer;
  border-bottom: 1px solid var(--border);
  border-radius: 0;
  transition: background 0.1s, color 0.1s;
  white-space: nowrap;
  margin-bottom: 0;
}
.settings-tab-dropdown .tab:last-child {
  border-bottom: none;
}
.settings-tab-dropdown .tab:hover {
  background: var(--bg-hover);
  color: var(--text-1);
}
.settings-tab-dropdown .tab.active {
  color: var(--accent);
  background: none;
  font-weight: 600;
  border-left: 3px solid var(--accent);
  box-shadow: none;
}

@media (max-width: 900px) {
  .settings-tabs {
    position: relative;
    border-bottom: none;
    margin-bottom: 16px;
  }
  .settings-tabs > .tab {
    display: none;
  }
  .settings-tab-toggle {
    display: flex;
  }
}
</style>

<div class="page-header">
  <div>
    <div class="page-eyebrow">{{ t('nav.system') }}</div>
    <div class="page-title">{{ t('nav.settings') }}</div>
    <div class="page-sub">{{ t('settings.subtitle') }}</div>
  </div>
</div>

<div class="settings-tabs">
  <button class="settings-tab-toggle" aria-expanded="false" aria-controls="settings-tab-dropdown">
    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
    <span class="settings-tab-label">{{ t('settings.tab_general') }}</span>
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-left:auto"><polyline points="6 9 12 15 18 9"/></svg>
  </button>
  <div class="settings-tab-dropdown" id="settings-tab-dropdown">
    <div class="tab active" data-settings-tab="general">{{ t('settings.tab_general') }}</div>
    <div class="tab" data-settings-tab="security">{{ t('settings.tab_security') }}</div>
    <div class="tab" data-settings-tab="backup">{{ t('settings.tab_backup') }}</div>
    <div class="tab" data-settings-tab="email">{{ t('settings.tab_email') }}</div>
    <div class="tab" data-settings-tab="monitoring">{{ t('settings.tab_monitoring') }}</div>
    <div class="tab" data-settings-tab="api">{{ t('settings.tab_api') }}</div>
    <div class="tab" data-settings-tab="advanced">{{ t('settings.tab_advanced') }}</div>
    <div class="tab" data-settings-tab="license">{{ t('settings.tab_license') }}</div>
  </div>
  <div class="tab active" data-settings-tab="general">{{ t('settings.tab_general') }}</div>
  <div class="tab" data-settings-tab="security">{{ t('settings.tab_security') }}</div>
  <div class="tab" data-settings-tab="backup">{{ t('settings.tab_backup') }}</div>
  <div class="tab" data-settings-tab="email">{{ t('settings.tab_email') }}</div>
  <div class="tab" data-settings-tab="monitoring">{{ t('settings.tab_monitoring') }}</div>
  <div class="tab" data-settings-tab="api">{{ t('settings.tab_api') }}</div>
  <div class="tab" data-settings-tab="advanced">{{ t('settings.tab_advanced') }}</div>
  <div class="tab" data-settings-tab="license">{{ t('settings.tab_license') }}</div>
</div>

{# ─── General Tab ─────────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="general">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 01-2.83 2.83l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z"/></svg>
      <span class="card-title">{{ t('settings.app_info') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="display:flex;justify-content:space-between;align-items:center;padding-bottom:11px;border-bottom:1px solid var(--border)">
        <span style="font-size:13px;color:var(--text-2)">{{ t('settings.app_name') }}</span>
        <code style="font-family:var(--font-mono);font-size:12px;font-weight:600">{{ appName }}</code>
      </div>
      <div style="display:flex;justify-content:space-between;align-items:center;padding-bottom:11px;border-bottom:1px solid var(--border)">
        <span style="font-size:13px;color:var(--text-2)">{{ t('settings.version') }}</span>
        <code style="font-family:var(--font-mono);font-size:12px">v{{ appVersion }}</code>
      </div>
      <div style="display:flex;justify-content:space-between;align-items:center;padding-bottom:11px;border-bottom:1px solid var(--border)">
        <span style="font-size:13px;color:var(--text-2)">{{ t('settings.wg_host') }}</span>
        <code style="font-family:var(--font-mono);font-size:12px;color:var(--accent)">{{ wgHost }}:{{ wgPort }}</code>
      </div>
      <div style="display:flex;justify-content:space-between;align-items:center;padding-bottom:11px;border-bottom:1px solid var(--border)">
        <span style="font-size:13px;color:var(--text-2)">{{ t('settings.subnet') }}</span>
        <code style="font-family:var(--font-mono);font-size:12px">{{ wgSubnet }}</code>
      </div>
      <div style="display:flex;justify-content:space-between;align-items:center">
        <span style="font-size:13px;color:var(--text-2)">{{ t('settings.default_language') }}</span>
        <code style="font-family:var(--font-mono);font-size:12px">{{ language }}</code>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 013 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>
      <span class="card-title">{{ t('data.title') or 'Data & Retention' }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="display:flex;gap:10px">
        <div class="form-group" style="flex:1;margin-bottom:0">
          <label class="form-label" for="data-traffic-days">{{ t('data.traffic_retention') or 'Traffic data (days)' }}</label>
          <input type="number" id="data-traffic-days" value="30" min="1" max="365" style="width:100%">
        </div>
        <div class="form-group" style="flex:1;margin-bottom:0">
          <label class="form-label" for="data-activity-days">{{ t('data.activity_retention') or 'Activity logs (days)' }}</label>
          <input type="number" id="data-activity-days" value="30" min="1" max="365" style="width:100%">
        </div>
      </div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="data-peer-timeout">{{ t('data.peer_timeout') or 'Peer online timeout (seconds)' }}</label>
        <input type="number" id="data-peer-timeout" value="180" min="30" max="600" style="width:100%">
        <small class="form-hint">{{ t('data.peer_timeout_hint') or 'Peer is considered offline after no handshake for this many seconds' }}</small>
      </div>
      <div id="data-message" style="display:none"></div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-data-save">{{ t('data.save') or 'Save' }}</button>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--red)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
      <span class="card-title">{{ t('settings.danger_zone') }}</span>
    </div>
    <div class="card-body">
      <button class="btn btn-ghost" style="color:var(--red);border-color:var(--red-bd);width:100%;justify-content:center" id="btn-clear-logs">
        {{ t('settings.clear_logs') }}
      </button>
    </div>
  </div>
</div>

{# ─── Security Tab ────────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="security" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>
      <span class="card-title">{{ t('security.title') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:14px">
      {# Account Lockout #}
      <div style="font-size:13px;font-weight:600">{{ t('security.lockout.title') }}</div>
      <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:13px">{{ t('security.lockout.enabled') }}</span>
        <div class="toggle on" id="security-lockout-enabled" data-managed="true"></div>
      </div>
      <div style="display:flex;gap:10px">
        <div class="form-group" style="flex:1;margin-bottom:0">
          <label class="form-label" for="security-lockout-attempts">{{ t('security.lockout.max_attempts') }}</label>
          <input type="number" id="security-lockout-attempts" value="5" min="1" max="100" style="width:100%">
        </div>
        <div class="form-group" style="flex:1;margin-bottom:0">
          <label class="form-label" for="security-lockout-duration">{{ t('security.lockout.duration') }}</label>
          <input type="number" id="security-lockout-duration" value="15" min="1" max="1440" style="width:100%">
        </div>
      </div>

      {# Locked accounts list #}
      <div style="font-size:12px;font-weight:600;color:var(--text-2)">{{ t('security.lockout.locked_accounts') }}</div>
      <div id="security-locked-list" style="font-size:12px;color:var(--text-3)">{{ t('security.lockout.no_locked') }}</div>

      <div style="border-top:1px solid var(--border);padding-top:14px;margin-top:2px">
        {# Password Complexity #}
        <div style="font-size:13px;font-weight:600;margin-bottom:10px">{{ t('security.password.title') }}</div>
        <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);margin-bottom:10px">
          <span style="font-size:13px">{{ t('security.password.enabled') }}</span>
          <div class="toggle" id="security-password-enabled" data-managed="true"></div>
        </div>
        <div class="form-group" style="margin-bottom:10px">
          <label class="form-label" for="security-password-min-length">{{ t('security.password.min_length') }}</label>
          <input type="number" id="security-password-min-length" value="8" min="4" max="128" style="width:100%">
        </div>
        <div style="display:flex;flex-direction:column;gap:8px">
          <div style="display:flex;align-items:center;justify-content:space-between;padding:6px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
            <span style="font-size:12px">{{ t('security.password.require_uppercase') }}</span>
            <div class="toggle on" id="security-password-uppercase" data-managed="true"></div>
          </div>
          <div style="display:flex;align-items:center;justify-content:space-between;padding:6px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
            <span style="font-size:12px">{{ t('security.password.require_number') }}</span>
            <div class="toggle on" id="security-password-number" data-managed="true"></div>
          </div>
          <div style="display:flex;align-items:center;justify-content:space-between;padding:6px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
            <span style="font-size:12px">{{ t('security.password.require_special') }}</span>
            <div class="toggle on" id="security-password-special" data-managed="true"></div>
          </div>
        </div>
      </div>

      <div id="security-message" style="display:none"></div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-security-save">{{ t('security.save') }}</button>
      </div>
    </div>
  </div>
</div>

{# ─── Backup Tab ──────────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="backup" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--text-2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
      <span class="card-title">{{ t('settings.backup') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:14px">
      <div style="font-size:12px;color:var(--text-3)">{{ t('settings.backup_subtitle') }}</div>
      <div style="display:flex;flex-direction:column;gap:8px;padding-bottom:14px;border-bottom:1px solid var(--border)">
        <div style="font-size:13px;font-weight:500">{{ t('settings.backup_download') }}</div>
        <div style="font-size:11px;color:var(--text-3)">{{ t('settings.backup_download_desc') }}</div>
        <button class="btn btn-primary" id="btn-backup-download" style="align-self:flex-start">
          {{ t('settings.backup_download') }}
        </button>
      </div>
      <div style="display:flex;flex-direction:column;gap:8px">
        <div style="font-size:13px;font-weight:500">{{ t('settings.restore') }}</div>
        <div style="font-size:11px;color:var(--text-3)">{{ t('settings.restore_desc') }}</div>
        <input type="file" id="backup-file-input" accept=".json" style="display:none">
        <div id="backup-preview" style="display:none;padding:10px;border-radius:6px;background:var(--bg-2);font-size:12px;font-family:var(--font-mono)"></div>
        <div id="restore-message" style="display:none"></div>
        <div style="display:flex;gap:8px">
          <button class="btn btn-ghost" id="btn-backup-select" style="flex:0 0 auto">
            {{ t('settings.restore_select') }}
          </button>
          <button class="btn btn-primary" id="btn-backup-restore" style="flex:0 0 auto;display:none">
            {{ t('settings.restore_confirm') }}
          </button>
        </div>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="8" rx="2"/><rect x="2" y="14" width="20" height="8" rx="2"/><line x1="6" y1="6" x2="6.01" y2="6"/><line x1="6" y1="18" x2="6.01" y2="18"/></svg>
      <span class="card-title">{{ t('autobackup.title') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('autobackup.subtitle') }}</div>

      <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:13px">{{ t('autobackup.enabled') }}</span>
        <div class="toggle" id="autobackup-enabled" data-managed="true"></div>
      </div>

      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="autobackup-schedule">{{ t('autobackup.schedule') }}</label>
        <select id="autobackup-schedule" style="width:100%;padding:8px 12px;font-size:12px">
          <option value="6h">{{ t('autobackup.schedule_6h') }}</option>
          <option value="12h">{{ t('autobackup.schedule_12h') }}</option>
          <option value="daily" selected>{{ t('autobackup.schedule_daily') }}</option>
          <option value="3d">{{ t('autobackup.schedule_3d') }}</option>
          <option value="weekly">{{ t('autobackup.schedule_weekly') }}</option>
        </select>
      </div>

      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="autobackup-retention">{{ t('autobackup.retention') }}</label>
        <input type="number" id="autobackup-retention" value="5" min="1" max="100" style="width:100%">
        <small class="form-hint">{{ t('autobackup.retention_hint') }}</small>
      </div>

      <div style="display:flex;justify-content:space-between;align-items:center;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:12px;color:var(--text-2)">{{ t('autobackup.directory') }}</span>
        <code style="font-family:var(--font-mono);font-size:11px">/data/backups/</code>
      </div>

      <div style="display:flex;justify-content:space-between;align-items:center;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:12px;color:var(--text-2)">{{ t('autobackup.last_run') }}</span>
        <span id="autobackup-last-run" style="font-size:11px;font-family:var(--font-mono);color:var(--text-3)">{{ t('autobackup.last_run_never') }}</span>
      </div>

      <div id="autobackup-message" style="display:none"></div>

      <div style="display:flex;gap:8px;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-ghost" id="btn-autobackup-run">{{ t('autobackup.run_now') }}</button>
        <button class="btn btn-primary" id="btn-autobackup-save">{{ t('autobackup.save') }}</button>
      </div>

      <div style="border-top:1px solid var(--border);padding-top:10px;margin-top:4px">
        <div style="font-size:12px;font-weight:600;color:var(--text-2);margin-bottom:8px">{{ t('autobackup.existing_files') }}</div>
        <div id="autobackup-files" style="font-size:12px;color:var(--text-3)">{{ t('autobackup.no_files') }}</div>
      </div>
    </div>
  </div>
</div>

{# ─── Email & SMTP Tab ───────────────────────────────── #}
<div class="settings-panel" data-settings-panel="email" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
      <span class="card-title">{{ t('route_auth.smtp_server_settings') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('route_auth.smtp_subtitle') }}</div>
      <div style="display:flex;gap:10px">
        <div class="form-group" style="flex:1;margin-bottom:0">
          <label class="form-label">{{ t('route_auth.smtp_host') }}</label>
          <input type="text" id="smtp-host" placeholder="smtp.gmail.com" style="width:100%">
        </div>
        <div class="form-group" style="width:100px;margin-bottom:0">
          <label class="form-label">{{ t('route_auth.smtp_port') }}</label>
          <input type="text" id="smtp-port" placeholder="587" style="width:100%">
        </div>
      </div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label">{{ t('route_auth.smtp_user') }}</label>
        <input type="text" id="smtp-user" placeholder="user@gmail.com" style="width:100%">
      </div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label">{{ t('route_auth.smtp_password') }}</label>
        <input type="password" id="smtp-password" style="width:100%">
        <small class="form-hint" id="smtp-password-hint" style="display:none"></small>
      </div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label">{{ t('route_auth.smtp_from') }}</label>
        <input type="email" id="smtp-from" placeholder="noreply@example.com" style="width:100%">
        <small class="form-hint">{{ t('route_auth.smtp_from_hint') }}</small>
      </div>
      <div style="display:flex;align-items:center;justify-content:space-between;padding:10px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <div>
          <div style="font-size:13px;font-weight:600">{{ t('route_auth.smtp_tls') }}</div>
          <div style="font-size:11px;color:var(--text-2);margin-top:2px">{{ t('route_auth.smtp_tls_desc') }}</div>
        </div>
        <div class="toggle on" id="smtp-tls" data-managed="true"></div>
      </div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-smtp-save">{{ t('route_auth.smtp_save') }}</button>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--text-2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 11 12 14 22 4"/><path d="M21 12v7a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2h11"/></svg>
      <span class="card-title">{{ t('route_auth.smtp_test') }}</span>
    </div>
    <div class="card-body">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:10px">{{ t('route_auth.smtp_test_desc') }}</div>
      <div style="display:flex;gap:8px">
        <input type="email" id="smtp-test-email" placeholder="{{ t('route_auth.smtp_test_email_placeholder') }}" style="flex:1">
        <button class="btn btn-ghost" id="btn-smtp-test">{{ t('route_auth.smtp_test_send') }}</button>
      </div>
      <div id="smtp-test-result" style="display:none;padding:8px 12px;border-radius:var(--radius-xs);font-size:12px;font-family:var(--font-mono);margin-top:10px"></div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
      <span class="card-title">{{ t('alerts.title') or 'Email Alerts' }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('alerts.subtitle') or 'Get notified by email when events occur. Requires SMTP to be configured.' }}</div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="alerts-email">{{ t('alerts.email') or 'Alert email address' }}</label>
        <input type="email" id="alerts-email" placeholder="admin@example.com" style="width:100%">
      </div>
      <div style="font-size:12px;font-weight:600;color:var(--text-2);margin-top:4px">{{ t('alerts.events_label') or 'Notify on these events:' }}</div>
      <div id="alerts-events" style="display:flex;flex-direction:column;gap:6px">
        {% set alertGroups = [
          { label: t('alerts.group_security') or 'Security', events: 'login_failed,account_locked,password_changed' },
          { label: t('alerts.group_peers') or 'Peers', events: 'peer_connected,peer_disconnected,peer_created,peer_deleted,peer_expired' },
          { label: t('alerts.group_routes') or 'Routes', events: 'route_down,route_up,route_created,route_deleted' },
          { label: t('alerts.group_system') or 'System', events: 'system_start,wg_restart,backup_restored,backup_reminder,resource_alert' }
        ] %}
        {% for g in alertGroups %}
        <label style="display:flex;align-items:center;gap:8px;padding:6px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer;font-size:12px">
          <input type="checkbox" class="alert-event-group" data-events="{{ g.events }}" style="accent-color:var(--accent)">
          <span>{{ g.label }}</span>
        </label>
        {% endfor %}
      </div>
      <div style="border-top:1px solid var(--border);padding-top:10px;margin-top:4px">
        <div style="font-size:12px;font-weight:600;color:var(--text-2);margin-bottom:8px">{{ t('alerts.extra_checks') or 'Periodic checks' }}</div>
        <div class="form-group" style="margin-bottom:8px">
          <label class="form-label" for="alerts-backup-days">{{ t('alerts.backup_reminder') or 'Backup reminder (days, 0=off)' }}</label>
          <input type="number" id="alerts-backup-days" value="0" min="0" max="365" style="width:100%">
        </div>
        <div style="display:flex;gap:10px">
          <div class="form-group" style="flex:1;margin-bottom:0">
            <label class="form-label" for="alerts-cpu">{{ t('alerts.cpu_threshold') or 'CPU alert (%, 0=off)' }}</label>
            <input type="number" id="alerts-cpu" value="0" min="0" max="100" style="width:100%">
          </div>
          <div class="form-group" style="flex:1;margin-bottom:0">
            <label class="form-label" for="alerts-ram">{{ t('alerts.ram_threshold') or 'RAM alert (%, 0=off)' }}</label>
            <input type="number" id="alerts-ram" value="0" min="0" max="100" style="width:100%">
          </div>
        </div>
      </div>
      <div id="alerts-message" style="display:none"></div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-alerts-save">{{ t('alerts.save') or 'Save' }}</button>
      </div>
    </div>
  </div>
</div>

{# ─── Monitoring Tab ──────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="monitoring" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
      <span class="card-title">{{ t('monitoring.settings_title') or 'Uptime Monitoring' }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="monitoring-interval">{{ t('monitoring.interval') or 'Check interval (seconds)' }}</label>
        <input type="number" id="monitoring-interval" value="60" min="10" max="3600" style="width:100%">
      </div>
      <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <div>
          <div style="font-size:13px;font-weight:600">{{ t('monitoring.email_alerts') or 'Email alerts' }}</div>
          <div style="font-size:11px;color:var(--text-2);margin-top:2px">{{ t('monitoring.email_alerts_desc') or 'Send email on status change' }}</div>
        </div>
        <div class="toggle" id="monitoring-email-alerts" data-managed="true"></div>
      </div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="monitoring-alert-email">{{ t('monitoring.alert_email') or 'Alert email address' }}</label>
        <input type="email" id="monitoring-alert-email" placeholder="admin@example.com" style="width:100%">
      </div>
      <div id="monitoring-message" style="display:none"></div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-monitoring-save">{{ t('monitoring.save') or 'Save' }}</button>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="22 12 18 12 15 21 9 3 6 12 2 12"/></svg>
      <span class="card-title">{{ t('metrics.title') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('metrics.subtitle') }}</div>
      <div style="display:flex;align-items:center;justify-content:space-between;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:13px">{{ t('metrics.enable') }}</span>
        <div class="toggle" id="metrics-enabled" data-managed="true"></div>
      </div>
      <div style="display:flex;justify-content:space-between;align-items:center;padding:8px 12px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm)">
        <span style="font-size:12px;color:var(--text-2)">{{ t('metrics.path') }}</span>
        <code style="font-family:var(--font-mono);font-size:11px">/metrics</code>
      </div>
      <div id="metrics-message" style="display:none"></div>
      <div style="display:flex;justify-content:flex-end;padding-top:4px">
        <button class="btn btn-primary" id="btn-metrics-save">{{ t('common.save') }}</button>
      </div>
    </div>
  </div>
</div>

{# ─── API & Webhooks Tab ─────────────────────────────── #}
<div class="settings-panel" data-settings-panel="api" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0110 0v4"/><circle cx="12" cy="16" r="1"/></svg>
      <span class="card-title">{{ t('tokens.title') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:12px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('tokens.subtitle') }}</div>
      <div id="tokens-list"></div>
      <div style="display:flex;flex-direction:column;gap:8px;padding-top:8px;border-top:1px solid var(--border)">
        <div class="form-group" style="margin-bottom:0">
          <label class="form-label">{{ t('tokens.name') }}</label>
          <input type="text" id="token-name" placeholder="{{ t('tokens.name_placeholder') }}" style="width:100%;padding:8px 12px;font-size:12px" maxlength="100">
        </div>
        <div class="form-group" style="margin-bottom:0">
          <label class="form-label">{{ t('tokens.scopes') }}</label>
          <div id="token-scopes" style="display:flex;flex-wrap:wrap;gap:6px">
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="read-only" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_read_only') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="full-access" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_full_access') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="peers" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_peers') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="routes" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_routes') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="settings" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_settings') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="webhooks" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_webhooks') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="logs" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_logs') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="system" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_system') }}
            </label>
            <label style="display:flex;align-items:center;gap:4px;font-size:11px;padding:4px 8px;background:var(--bg-base);border:1px solid var(--border);border-radius:var(--radius-sm);cursor:pointer">
              <input type="checkbox" value="backup" class="token-scope-cb" style="accent-color:var(--accent)">
              {{ t('tokens.scope_backup') }}
            </label>
          </div>
        </div>
        <div class="form-group" style="margin-bottom:0">
          <label class="form-label">{{ t('tokens.expires') }}</label>
          <select id="token-expires" style="width:100%;padding:8px 12px;font-size:12px">
            <option value="">{{ t('tokens.expires_never') }}</option>
            <option value="30d">{{ t('tokens.expires_30d') }}</option>
            <option value="90d">{{ t('tokens.expires_90d') }}</option>
            <option value="1y">{{ t('tokens.expires_1y') }}</option>
            <option value="custom">{{ t('tokens.expires_custom') }}</option>
          </select>
          <input type="date" id="token-expires-custom" style="display:none;width:100%;padding:8px 12px;font-size:12px;margin-top:6px">
        </div>
        <div id="token-created-result" style="display:none"></div>
        <div id="tokens-message" style="display:none"></div>
        <button class="btn btn-primary" id="btn-create-token" style="align-self:flex-start">
          {{ t('tokens.add') }}
        </button>
      </div>
    </div>
  </div>

  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--text-2)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9"/><path d="M13.73 21a2 2 0 01-3.46 0"/></svg>
      <span class="card-title">{{ t('webhooks.title') }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:12px">
      <div style="font-size:12px;color:var(--text-3);margin-bottom:4px">{{ t('webhooks.subtitle') }}</div>
      <div id="webhooks-list"></div>
      <div style="display:flex;flex-direction:column;gap:8px;padding-top:8px;border-top:1px solid var(--border)">
        <input type="text" id="webhook-url" placeholder="{{ t('webhooks.url_placeholder') }}" style="width:100%;padding:8px 12px;font-size:12px" maxlength="500">
        <input type="text" id="webhook-desc" placeholder="{{ t('webhooks.description_placeholder') }}" style="width:100%;padding:8px 12px;font-size:12px" maxlength="255">
        <button class="btn btn-primary" id="btn-add-webhook" style="align-self:flex-start">
          {{ t('webhooks.add') }}
        </button>
      </div>
    </div>
  </div>
</div>

{# ─── Advanced Tab ────────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="advanced" style="display:none">
  <div class="card">
    <div class="card-head">
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="var(--accent)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/></svg>
      <span class="card-title">{{ t('ip2location.title') or 'ip2location (Geo-IP)' }}</span>
    </div>
    <div class="card-body" style="display:flex;flex-direction:column;gap:11px">
      <div style="font-size:12px;color:var(--text-3)">{{ t('ip2location.subtitle') or 'API key for country-based IP access control. Get a free key at ip2location.io' }}</div>
      <div class="form-group" style="margin-bottom:0">
        <label class="form-label" for="ip2location-key">{{ t('ip2location.api_key') or 'API Key' }}</label>
        <input type="password" id="ip2location-key" style="width:100%">
      </div>
      <div style="display:flex;gap:8px">
        <button class="btn btn-primary" id="btn-ip2location-save" style="font-size:12px">{{ t('ip2location.save') or 'Save' }}</button>
        <button class="btn btn-ghost" id="btn-ip2location-test" style="font-size:12px">{{ t('ip2location.test') or 'Test' }}</button>
      </div>
      <div id="ip2location-result" style="display:none"></div>
    </div>
  </div>
</div>

{# ─── License Tab ─────────────────────────────────────── #}
<div class="settings-panel" data-settings-panel="license" style="display:none">
  <!-- Status Card -->
  <div class="card" style="margin-bottom:20px">
    <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px">
      <h3 style="margin:0">{{ t('license.status') }}</h3>
      <span class="badge {% if license.plan == 'pro' %}badge-green{% elif license.plan == 'lifetime' %}badge-blue{% else %}badge-grey{% endif %}">
        {{ t('license.plan_' + license.plan) }}
      </span>
    </div>
    {% if license.expires_at %}
      <p style="margin:4px 0;color:var(--text-2)">{{ t('license.valid_until') }}: {{ license.expires_at }}</p>
    {% endif %}
    {% if license.activations %}
      <p style="margin:4px 0;color:var(--text-2)">{{ t('license.activations') }}: {{ license.activations }} / {{ license.max_activations }}</p>
    {% endif %}
    {% if license.plan == 'community' %}
      <p style="margin:12px 0 0;color:var(--text-2)">{{ t('license.community_hint') }}</p>
    {% endif %}
  </div>

  <!-- Activate / Key Input -->
  <div class="card" style="margin-bottom:20px">
    <h3 style="margin:0 0 16px">{% if license.license_key_masked %}{{ t('license.key_label') }}: {{ license.license_key_masked }}{% else %}{{ t('license.activate') }}{% endif %}</h3>
    <form id="license-form">
      <div style="margin-bottom:12px">
        <label style="font-size:13px;font-weight:600;display:block;margin-bottom:4px">{{ t('license.key_label') }}</label>
        <input type="text" name="license_key" placeholder="{{ t('license.key_placeholder') }}" class="input" style="width:100%">
      </div>
      <div style="margin-bottom:12px">
        <label style="font-size:13px;font-weight:600;display:block;margin-bottom:4px">{{ t('license.signing_key_label') }}</label>
        <input type="password" name="signing_key" placeholder="{{ t('license.signing_key_placeholder') }}" class="input" style="width:100%">
      </div>
      <div style="display:flex;gap:8px">
        <button type="submit" class="btn btn-primary">{{ t('license.activate') }}</button>
        {% if license.license_key_masked %}
          <button type="button" class="btn btn-secondary" id="license-refresh-btn">{{ t('license.refresh') }}</button>
          <button type="button" class="btn btn-danger" id="license-remove-btn">{{ t('license.remove') }}</button>
        {% endif %}
      </div>
    </form>
  </div>

  <!-- Feature Overview -->
  <div class="card">
    <h3 style="margin:0 0 16px">{{ t('license.features') }}</h3>
    <table style="width:100%;font-size:13px">
      <thead>
        <tr style="text-align:left;border-bottom:1px solid var(--border)">
          <th style="padding:8px 0">Feature</th>
          <th style="padding:8px 0">{{ t('license.status') }}</th>
        </tr>
      </thead>
      <tbody>
        {% for key, value in license.features %}
        <tr style="border-bottom:1px solid var(--border-light)">
          <td style="padding:8px 0">{{ key | replace("_", " ") | capitalize }}</td>
          <td style="padding:8px 0">
            {% if value == true %}
              <span style="color:var(--green)">{{ t('license.feature_enabled') }}</span>
            {% elif value == false %}
              <span style="color:var(--text-3)">{{ t('license.feature_disabled') }}</span>
            {% elif value == -1 %}
              <span style="color:var(--green)">{{ t('license.unlimited') }}</span>
            {% elif value == 0 %}
              <span style="color:var(--text-3)">{{ t('license.feature_disabled') }}</span>
            {% else %}
              <span>{{ value }}</span>
            {% endif %}
          </td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>

{% endblock %}

{% block scripts %}
<script src="/js/settings.js"></script>
<script src="/js/tokens.js"></script>
{% endblock %}
